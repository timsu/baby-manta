node_modules/
dist/
build/
coverage/
*.tsbuildinfo
# Env files are NEVER committed — not even dotenvx-encrypted ones. Ciphertext in
# a repo is only as safe as the private key, and the key outlives the repo.
# `.env.example` is the one exception: it holds names and placeholders only.
.env
.env.*
*.local
.env.keys
!.env.example
.DS_Store
*.log
.vite/
playwright-report/
test-results/
# Prisma generated client
packages/db/generated/
# SQLite database files (local data, never committed)
*.db
*.db-journal
*.db-wal
*.db-shm
.worker.env
.claude
.manta-images/
.pi/

# Scratch/test artifacts
.tmp/
