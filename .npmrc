# Manta pnpm config. (Workspace linking + peer auto-install are pnpm 10 defaults.)

# Disable store integrity checks — pnpm 10.x has a known bug where integrity
# verification returns undefined in fresh git worktrees, crashing the install.
verify-store-integrity=false
